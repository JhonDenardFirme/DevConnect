"use server";

export const handleSignUp = async (data) => {
    try {
      console.log("Form Data:", data); // Debugging
      // Send data to your API or authentication system
    } catch (error) {
      console.error("Error signing up:", error);
    }
  };
  